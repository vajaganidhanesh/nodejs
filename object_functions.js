let students = {
    name : 'dhanesh',
    age  : 24,
    desigation : 'Frontend developer',
}

let keys = Object.keys(students)
let values = Object.values(students)
let entires = Object.entries(students)

console.log(entires);
