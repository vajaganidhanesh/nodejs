var str = "Hello, World!";
var index = 1; // Index of the character you want to retrieve

var charCode = str.charCodeAt(index);

console.log("Character at index " + index + " is: " + str[index]);
console.log("Unicode value (char code) at index " + index + " is: " + charCode);
